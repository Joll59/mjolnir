import { GameMapReducer } from '../../reducers/GameMap';

describe(
    'Game Map Reducer', () => {
        it( 'is Defined', () => {
            expect(GameMapReducer).toBeDefined();
        });
    });